<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.ShiftMapper">

<insert id="ShiftsAdd" parameterType="Shifts">
<!-- 指定日の希望シフト一覧から確定シフト挿入処理 -->
insert into shifts values(null,#{userId},#{startDateTime},#{endDateTime},#{isApproved},#{shiftType},#{breakDuration},#{dayOfWeek})

</insert>

<select  id="selectShiftAll" resultType="Shifts">
<!-- カレンダー日付ごとの確定シフト一覧 -->
</select>

</mapper>
